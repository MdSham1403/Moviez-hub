{
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "sleep 5 && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn Moviezhub.wsgi:application --bind 0.0.0.0:$PORT"
  }
}